# Lucky Club Gift Articles

Lucky Club Gift Articles is a Next.js e-commerce website for personalized gifts.

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000` to view the application.

## Production

Production database migrations are managed in Supabase. The Vercel build runs the Next.js production build without requiring a database connection during the build step.
